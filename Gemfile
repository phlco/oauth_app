# A sample Gemfile created with `$ bundle init`

# URL for a Rubygems server where we'll download our gems
source "https://rubygems.org"

# Run `$ bundle install` to install these gems
gem 'sinatra'
gem 'pry'
gem 'httparty'
